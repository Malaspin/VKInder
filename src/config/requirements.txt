# VK Bot зависимости
# Переход на VKBottle (асинхронный фреймворк)
vkbottle>=3.3.0
aiohttp>=3.9.0
requests>=2.20.0
psutil>=5.8.0

# Database зависимости  
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
asyncpg>=0.29.0  # Драйвер для асинхронного SQLAlchemy (PostgreSQL)
pydantic>=2.0.0
loguru>=0.7.0

# Encryption зависимости
cryptography>=41.0.0

# Общие зависимости (берем более свежую версию)
python-dotenv>=1.1.1

# Старые зависимости (закомментированы, будут удалены после полной миграции)
# vk_api==11.10.0
